# Custom vectorization implementation

This runnable sample contains the embedding and reranker adapters, benchmark
scripts, Kubernetes manifests, and architecture source files used by the custom
vectorization topic.
