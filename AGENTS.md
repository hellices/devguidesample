# 문서 작성 규칙 — 이 저장소에 문서를 추가할 때

> 사람이 쓸 때도, AI 에이전트에게 시킬 때도 이 파일 하나를 봅니다.
> 규칙은 취향이 아니라 **기존 문서 78개를 세어서 나온 다수 관행**입니다. 근거 숫자를 함께 적어 두었습니다.
> 기계로 검사할 수 있는 항목은 [`scripts/check_docs.py`](scripts/check_docs.py)가 대신 봅니다.

---

## 📌 핵심 요약

| # | 규칙 | 왜 |
|---|---|---|
| 1 | 설명이 표를 데려오게 — 표를 연달아 쌓지 않기 | 결론이 셀 안에 들어가면 왜 그런지가 문장에서 사라짐 |
| 2 | 문서 하나에 문체 하나 — 절차·실습은 합니다체, 리서치·검증은 한다체 | 기존 54개 중 53개가 한 문서 안에서 하나만 사용 |
| 3 | 표 셀도 절 제목도 명사구로, 종결어미 없이 | 한글 표 셀 4,012개 중 97.1%가 명사구 |
| 4 | 문서가 길어지면 하위 디렉터리 + `README.md` 허브 + `NN-` 번호 파일 | `memory/agent-memory/`, `monitor/sre-agent-event-lab/guides/` 선례 |
| 5 | 절 번호는 문서마다 1부터, 다른 문서를 가리킬 때는 문서 이름을 함께 | 번호만 쓰면 어느 문서의 3절인지 알 수 없음 |
| 6 | 측정값에는 **규모·리전·날짜**를 붙이고, 고객 규모로 잴 것 | 작게 재서 외삽하면 규모에서만 나오는 실패 모드를 놓침 |
| 7 | 제품 사양은 Microsoft Learn에서 확인해 링크. 면책 문장은 쓰지 않기 | 사양은 바뀌고 저장소 문서는 따라가지 못함 |
| 8 | 자격 증명·고객 식별 정보 금지 | 이 저장소는 **공개**(public) |
| 9 | 올리기 전 `gen_index.py` + `check_docs.py` 실행 | 루트 README 인덱스와 링크·앵커는 손으로 맞추지 않음 |

---

## 1. 어디에 두는가

Azure 서비스 이름의 카테고리 디렉터리 아래에 둡니다 (`aks/`, `redis/`, `monitor/`, `aisearch/` …).
해당하는 카테고리가 없으면 새로 만들면 됩니다. **루트 `README.md`의 목록은 건드리지 마세요** — 자동 생성됩니다 (7절).

### 1.1 낱개 문서

카테고리 바로 아래에 `.md` 파일 하나로 둡니다. 파일명은 소문자와 하이픈 또는 밑줄을 씁니다.

```
redis/nodejs/cluster_failover_recovery.md
monitor/azure-sre-agent.md
```

### 1.2 묶음 문서 — 나누는 기준

한 문서가 500줄을 넘어가거나 "고를 때 보는 것"과 "실행할 때 보는 것"이 한 파일에 섞이면 나눕니다.
**하위 디렉터리를 만들고, 허브 `README.md`와 번호가 붙은 문서로 나눕니다.**

```
memory/agent-memory/
├── README.md                    # 허브 — 30초 요약, 문서 구성 표, 읽는 순서
├── 01-memory-taxonomy.md
├── 02-architecture-patterns.md
└── images/*.svg

redis/
├── azure-cache-to-managed-redis-migration.md   # 판단에 필요한 것만
└── migration-guide/
    ├── 01-differences.md
    ├── 02-client-audit.md
    └── 03-migration-paths.md
```

허브에는 최소한 다음을 둡니다. `memory/agent-memory/README.md`가 이 형태의 기준입니다.

| 절 | 담는 것 |
|---|---|
| 30초 요약 (또는 핵심 요약) | 문서를 안 읽고 나가는 사람이 가져갈 결론, 표 형태 |
| 문서 구성 | `문서 \| 내용 \| 대상` 표. 각 문서가 무엇을 담고 누가 읽는지 |
| 읽는 순서 | 목적별 경로 — "시간이 없다면", "구현에 들어간다면" |
| 참고 자료 | 번호 목록. 링크 뒤에 `—`로 무엇인지 한 줄 |

**파일 번호와 문서 안의 절 번호는 별개입니다.** `03-migration-paths.md`의 첫 절은 `## 1.`입니다.

### 1.3 실습 랩

Azure 리소스를 실제로 배포하는 실습은 `azure.yaml`과 `README.md`를 갖춘 디렉터리로 만듭니다.
두 파일이 있으면 루트 README의 실습 랩 표에 자동으로 들어갑니다.
랩 README에는 **사전 조건, 배포, 검증, 정리 절차**가 모두 있어야 합니다. 정리 절차가 없는 랩은 올리지 마세요.

---

## 2. 문서의 형태

### 2.1 제목

H1은 **Azure 서비스 이름을 담습니다.** 부제는 선택이고, 붙인다면 em dash(`—`)를 씁니다 (부제 없음 43 : em dash 12 : 콜론 9 : 괄호 9).

```markdown
# Azure Cache for Redis → Azure Managed Redis 마이그레이션
# 01. 메모리 분류 체계 — 무엇을 기억할 것인가
```

묶음 안의 번호 문서는 `# NN. 제목 — 부제` 형태로 파일 번호를 제목에 넣습니다.

**절 제목(H2·H3)은 명사구로 씁니다.** 종결어미로 끝나는 문장형 제목은 목차에서 읽기 어렵고,
앵커도 길어집니다. 결론을 담는 것은 괜찮습니다 — 문장이 아니라 명사구로 담으면 됩니다.

```markdown
### 2.5 EnterpriseCluster에 남는 크로스 슬롯 제약     <!-- 이렇게 -->
### 2.5 EnterpriseCluster도 크로스 슬롯 제약이 남습니다  <!-- 이렇게 쓰지 않습니다 -->

### 5. 용량 산정 — 두 번의 착시     <!-- 이렇게 -->
### 5. 용량 산정 — 두 번 속습니다   <!-- 이렇게 쓰지 않습니다 -->
```

**의문형 제목은 예외입니다.** `## 2. 무엇을 고를 것인가`, `## 4. REPLICAOF는 왜 안 되는가`처럼
읽는 사람이 그 자리에서 갖고 있을 질문을 그대로 쓴 제목은 명사구로 바꾸지 않습니다.

**제목은 다루는 주제를 가리킵니다. 문서 자체를 가리키지 않습니다.**
`이 문서`, `이 묶음`, `…와의 대응`, `…에 대한 답`처럼 문서의 구조나 다른 문서와의 관계를 제목에 올리면
목차만 봐서는 그 절에 무엇이 적혀 있는지 알 수 없습니다. 그 절이 설명하는 것을 그대로 쓰면 됩니다.

```markdown
## 1. 공식 마이그레이션 가이드 3단계             <!-- 이렇게 -->
## 1. 공식 마이그레이션 방법론과 이 묶음의 대응    <!-- 이렇게 쓰지 않습니다 -->

## 7. 측정하지 않은 것            <!-- 이렇게 -->
## 7. 이 문서가 측정하지 않은 것   <!-- 이렇게 쓰지 않습니다 -->

## 3. 클라이언트 무수정 가능 범위와 다운타임 하한       <!-- 이렇게 -->
## 3. "클라이언트 수정 없이, 다운타임 없이"에 대한 답    <!-- 이렇게 쓰지 않습니다 -->
```

**문서 구성·읽는 순서 같은 길잡이는 번호 절 안에 넣지 않고 앞머리에 H2로 둡니다** (1.2절).
번호 절은 주제를 다루는 자리이고, 길잡이는 그 앞에 서는 자리입니다.

### 2.2 첫 절과 마지막 절

첫 H2는 다음 중 하나를 씁니다 — `📌 핵심 요약`(7건) · `개요`(6건) · `시작 조건`(5건).
마지막 H2는 `## 참고` 또는 `## 참고 자료`입니다 (35건). 링크 뒤에 `—`로 무엇을 볼 때 쓰는지 한 줄 적습니다.

### 2.3 그 사이

- H2 사이에 `---` 구분선 (37/78건)
- H1 바로 다음에 `>` 인용문으로 이 문서가 무엇인지 한두 줄 (22/78건)
- 이모지는 H2 앞에만, 그것도 드물게. 본문에는 쓰지 않습니다
- 다이어그램이 있으면 H1 바로 다음. SVG를 권장합니다

---

## 3. 어떻게 설명하는가

이 저장소의 문서는 사실이 촘촘한데도 읽기 어렵다는 지적을 받은 적이 있습니다.
내용이 틀려서가 아니라 **배치 때문이었습니다.** 결론을 표 셀에 넣고 그 표를 연달아 쌓으면,
읽는 사람은 각 칸을 조회할 수는 있어도 왜 그런 결론이 나왔는지는 따라갈 수 없습니다.
사양서는 그래도 되지만, 판단을 도우려고 쓴 문서는 그러면 안 됩니다.

### 3.1 표를 데려오는 설명

**한 절을 표로 시작하지 마세요.** 표는 앞 문장이 던진 질문에 답하는 자리에 놓입니다.

```markdown
<!-- 이렇게 쓰지 않습니다 -->
### 2.3 clusteringPolicy 세 가지
| 정책 | 클라이언트가 보는 것 | 비고 |
...

<!-- 이렇게 씁니다 -->
### 2.3 clusteringPolicy 세 가지
AMR은 항상 클러스터인데, 그 클러스터를 클라이언트에게 어떻게 보여 줄지는 고를 수 있습니다.
선택지가 셋이고, 셋의 차이는 결국 "클라이언트를 바꿔야 하는가"로 수렴합니다.
| 정책 | 클라이언트가 보는 것 | 비고 |
...
```

두 문장이 하는 일은 표에 없는 것을 주는 것입니다 — **이 표를 왜 보고 있는지, 다 읽고 나면 무엇이 남는지.**

### 3.2 결론까지 가는 길 — 결론만 놓지 않기

제목에는 결론을 담아도 됩니다 (`2.5 EnterpriseCluster에 남는 크로스 슬롯 제약`).
다만 **본문의 첫 문장은 읽는 사람이 그 자리에서 갖고 있을 질문에서 출발합니다.**
"단일 엔드포인트로 보인다니 그냥 쓰면 되겠네"라고 생각하며 도착한 사람에게는,
그 기대를 한 번 짚어 준 다음 어디서 어긋나는지 보여 주는 편이 결론을 먼저 못 박는 것보다 잘 남습니다.

막다른 길을 확인했다면 **막다른 길이라는 결과만 적지 말고 어디까지 가 봤는지를 적으세요.**
다음 사람이 같은 길을 다시 걷지 않는 것은 결론이 아니라 그 경로가 막아 줍니다.

### 3.3 절과 절의 연결

새 절을 시작할 때 **앞 절에서 무엇이 정해졌고 그래서 이 절이 왜 나오는지** 한 문장으로 받습니다.
문서가 절의 목록이 아니라 하나의 이야기가 되는 것은 대부분 이 한 문장 덕분입니다.

> [ACR과 AMR의 차이](redis/migration-guide/01-differences.md) 2절의 결론은
> "우리 애플리케이션이 허용 목록 밖의 다중 키 명령을 쓰는가"를 확인하라는 것이었다. 이 문서가 그 확인 목록이다.

### 3.4 강조의 절약

한 문단에서 굵게 쓰는 것은 **하나**입니다. 전부 굵으면 아무것도 굵지 않고,
읽는 사람은 굵은 조각만 이어 붙여 읽다가 문장을 통째로 건너뜁니다.
굵게 쓸 자리는 **읽는 사람이 그것 하나만 가져가도 되는 문장**입니다.

### 3.5 문체 — 문서 하나에 하나

**기존 문서 54개 중 53개가 한 문서 안에서 한 문체만 씁니다.** 섞지 마세요.

문체를 가르는 기준은 문서의 형식이 아니라 **글이 하는 일**입니다.
읽는 사람에게 무엇을 하라고 시키는 글인지, 무엇이 어떠하더라고 알려 주는 글인지로 갈립니다.

| 문서 성격 | 문체 | 예 |
|---|---|---|
| 시키는 글 — 실습 랩, 단계별 절차, 트러블슈팅 런북 | 합니다체 | "AMR을 `EnterpriseCluster`로 생성합니다" |
| 알려 주는 글 — 리서치, 벤치마크, 비교 분석, **실측 기반 마이그레이션 가이드** | 한다체 | "복사가 도는 동안 들어온 쓰기는 타깃에 반영되지 않는다" |

**"가이드"라는 이름만으로 합니다체가 되지 않습니다.** 절차가 몇 개 섞여 있더라도
글의 대부분이 "무엇이 왜 그런가"를 설명하고 있으면 한다체를 씁니다
(`redis/azure-cache-to-managed-redis-migration.md` 묶음이 이 경우입니다).

묶음 문서는 **묶음 전체가 같은 문체**를 씁니다. 허브만 한다체이고 상세는 합니다체이면 안 됩니다.

용어는 아래로 고정합니다. `check_docs.py`가 오류로 잡습니다.

- `디렉터리` — `디렉토리`로 쓰지 않습니다 <!-- check-docs:ignore -->
- `애플리케이션` — `어플리케이션`으로 쓰지 않습니다 <!-- check-docs:ignore -->

틀린 표기를 일부러 적어야 하는 줄에는 위처럼 `<!-- check-docs:ignore -->`를 붙입니다.

---

## 4. 표

### 4.1 표에 넣을 것 — 조회할 자료만

표가 잘 맞는 것은 **축이 뚜렷하고 값이 여러 개인 것**입니다. SKU별 한도, 명령별 성공·실패,
항목별 지원 여부처럼 읽는 사람이 자기 경우를 찾아 한 줄만 보고 나갈 수 있는 자료입니다.

반대로 **왜 그런지와 그래서 무엇을 해야 하는지는 표에 넣지 않습니다.** 셀에 들어가는 순간
앞뒤 문장과 끊어지고, 읽는 사람은 근거 없이 지시만 받게 됩니다.

판별법이 하나 있습니다. **표를 지워 보고 남은 문장만 읽어 보세요.**
그래도 결론이 서면 그 표는 근거입니다. 결론이 같이 사라지면 그 표는 논증을 칸 속에 숨긴 것이고,
문장으로 꺼내야 합니다.

### 4.2 설명형 2열 표 — 명명된 불릿으로

4.1의 판별법에 걸리는 표는 대개 모양이 같습니다. **왼쪽 열에 이름이 있고 오른쪽 열에 설명이 있는 2열 표**입니다.
축이 하나뿐이라 조회할 것이 없고, 오른쪽 칸의 설명은 명사구로 줄이느라 문장의 절반이 잘려 나갑니다.

이런 표는 **`**이름**: 설명 문장.` 형태의 불릿**으로 풀어 씁니다. 칸 폭 제약이 사라지므로
설명을 완결된 문장으로 쓸 수 있고, 문장 여러 개를 이어 붙일 수도 있습니다.

```markdown
<!-- 이렇게 쓰지 않습니다 -->
| 제약 | 내용 |
|---|---|
| 전환 시점 통제 불가 | 마이그레이션 시작만 선택 가능 |
| 전체 동시 전환 | 모든 애플리케이션이 동시에 전환됨 |

<!-- 이렇게 씁니다 -->
- **전환 시점 통제 불가**: 마이그레이션 시작만 고를 수 있고, 실제 트래픽 전환 시점은 고를 수 없다.
- **전체 동시 전환**: 하나의 Redis에 붙은 **모든** 애플리케이션이 동시에 전환된다. 서비스 단위 점진 전환이 안 된다.
```

**조회용 매트릭스는 그대로 표로 둡니다.** 명령별 성공·실패, SKU별 한도, 항목별 지원 여부처럼
열이 셋 이상이거나 같은 축의 값을 여러 줄에서 비교하는 표가 여기 해당합니다.

### 4.3 볼드 리드-인 — 이름 자리에만

불릿 앞머리의 볼드는 **훑는 사람이 찾을 대상**을 세우는 자리입니다. 4.2가 만드는 `**이름**: 설명.`이
그 형태이고, 저장소의 볼드 리드-인 376건 중 335건이 이렇게 이름으로 끝납니다.

나머지 41건은 **문장 전체가 볼드 안에 들어가 있습니다** — `- **출발점은 제품 계보 하나다.** 두 서비스가…`처럼
서술어로 끝나는 모양입니다. 이러면 찾을 대상이 없고 볼드가 말하는 것은 "이 문장은 중요하다" 하나뿐입니다.

판별법은 4.1과 결이 같습니다. **한 목록의 불릿이 전부 같은 자리에 볼드를 달고 있으면 그 볼드는 신호가 아닙니다.**
전부 중요하다는 표시는 아무것도 표시하지 않습니다. 반대로 평문 불릿 사이에 하나나 둘만 볼드면 그건 진짜 강조이므로 둡니다.

균일하게 볼드가 붙은 목록을 만나면 둘 중 하나로 보냅니다.

- 훑어서 찾을 대상이면 **명사구 이름으로 줄이고** 콜론 뒤에 설명을 답니다 (4.2 형태).
- 찾을 대상이 아니면 **볼드를 뺍니다.** 주장은 문장이 이미 하고 있습니다.

```markdown
<!-- 이렇게 쓰지 않습니다 — 세 불릿이 전부 문장 볼드 -->
- **출발점은 제품 계보 하나다.** 두 서비스가 다른 소프트웨어 위에 서 있다는 사실 말고는 가정하지 않는다.
- **중간 고리는 선택으로 끊을 수 없다.** AMR에서 클러스터는 전제다.

<!-- 찾을 대상이면 이름으로 줄입니다 -->
- **출발점**: 제품 계보 하나다. 두 서비스가 다른 소프트웨어 위에 서 있다는 사실 말고는 가정하지 않는다.
- **중간 고리**: 선택으로 끊을 수 없다. AMR에서 클러스터는 전제다.

<!-- 찾을 대상이 아니면 볼드를 뺍니다 -->
- 문서와 실측이 정확히 맞았다. 허용 목록 6개와 통과 항목이 한 개도 어긋나지 않았다.
```

### 4.4 셀의 끝 — 명사구

한글 표 셀 4,012개 중 97.1%가 그렇습니다.

- `키 개수에 거의 선형` — `키 개수에 거의 선형입니다` 아님
- `HA 복제본이 합산됨` — `HA 복제본이 합산됩니다` 아님
- `소스를 지우면 끝` — `소스를 지우면 끝이다` 아님

예외는 셀 하나가 정의문 전체인 경우입니다. 그때는 문서의 문체를 따릅니다 (`memory/agent-memory/01-memory-taxonomy.md`의 8유형 정의표).
셀 안에 `|`를 쓸 일이 있으면 `\|`로 이스케이프합니다.

---

## 5. 링크와 절 번호

- 저장소 안은 **상대 경로**로 링크합니다. GitHub URL을 쓰지 마세요
- 앵커(`#절-제목`)는 대상 문서의 실제 제목과 맞아야 합니다. 제목을 고치면 그 제목을 가리키는 링크도 같이 고칩니다
- **같은 문서에 같은 제목의 H2/H3를 두지 마세요.** 앵커가 갈라집니다
- 링크 텍스트는 대상 문서의 이름을 담습니다. "여기", "이 문서", "가이드"만 쓰면 어디로 가는지 알 수 없습니다

**절 번호는 문서마다 1부터 매깁니다.** 다른 문서의 절을 가리킬 때는 문서 이름을 함께 씁니다.

```markdown
[ACR과 AMR의 차이 2.6절](migration-guide/01-differences.md#26-정책-변경의-방향--nocluster에서-나오는-길-하나)
```

묶음의 상세 문서에는 상단에 소속을 밝히는 인용문을 둡니다. 문체는 그 묶음의 문체를 따릅니다.

```markdown
> 이 문서는 [ACR → AMR 마이그레이션 가이드](../azure-cache-to-managed-redis-migration.md)의 상세 문서다.
> **절 번호는 문서마다 1부터 매긴다.** 다른 문서를 가리킬 때는 문서 이름을 함께 쓴다.
```

---

## 6. 내용

### 6.1 측정값

숫자를 적으려면 **어떤 규모에서, 어느 리전에서, 언제** 쟀는지 함께 적습니다. 조건 없는 숫자는 다음 사람이 쓸 수 없습니다.

```markdown
> Korea Central에 실제 리소스를 만들어 **3.77GB / 215만 키** 규모로 측정했다 (2026-08-27 KST).
```

- **고객 규모로 재세요.** 작게 재서 외삽하면, 규모에서만 나타나는 실패 모드를 통째로 놓칩니다
- 한 번 재고 끝내지 말고 **여러 번 돌려 재현되는지** 확인합니다
- 재지 않은 것은 **재지 않았다고 적습니다.** 설계 지침으로만 읽으라고 명시하세요
- 숫자를 자극적으로 쓰지 않습니다. "48.47% 유실"은 조건과 함께 놓일 때만 사실입니다
- 스크립트와 원본 데이터를 함께 올려 재현할 수 있게 합니다

### 6.2 공식 문서와의 관계

**제품 사양의 기준은 Microsoft Learn입니다.** 사양은 바뀌고, 저장소 문서는 따라가지 못합니다.

- 제품 사양·제약·지원 여부를 쓸 때는 Microsoft Learn에서 확인하고 그 페이지를 링크합니다
- 공식 가이드가 있는 주제라면 **어느 문서가 기준인지 본문에 밝히고**, 이 문서가 무엇을 보태는지 적습니다
- 공식 문서와 실측이 어긋나면 양쪽을 다 적습니다. 어느 한쪽을 지우지 마세요

**다만 이것을 면책 문장으로 쓰지는 않습니다.** "이 문서는 공식 가이드를 대체하지 않는다",
"먼저 읽어야 할 것은 공식 문서다", "이 묶음은 보조 자료다" 같은 문장은 정보를 주지 않고 자리만 차지합니다.
공식 방법론이 무엇인지 그대로 적고 이 문서가 그 어디에 대응하는지 보이면, 기준이 어느 쪽인지는 저절로 드러납니다.

```markdown
<!-- 이렇게 쓰지 않습니다 -->
이 문서는 Microsoft 공식 마이그레이션 가이드를 대체하지 않는다. 먼저 읽어야 할 것은 공식 문서이고,
이 묶음은 거기에 적히지 않은 것을 채우는 보조 자료다.

<!-- 이렇게 씁니다 -->
Microsoft의 공식 방법론은 3단계다 — 차이를 이해하고, 경로를 고르고, 실행 계획을 세운다.
이 묶음은 각 단계에 그대로 대응하면서 공식 문서가 숫자로 적지 않은 것을 채운다.
```

### 6.3 올리면 안 되는 것

이 저장소는 **공개**입니다. 커밋 전에 diff를 눈으로 확인하세요.

- 액세스 키, 연결 문자열, 토큰, 구독 ID, 테넌트 ID
- 고객사 이름, 실제 리소스 이름, 실제 엔드포인트 호스트명
- 사내 시스템 URL, 내부 티켓 번호

리소스 이름은 `<name>.<region>.redis.azure.net`처럼 자리표시자로 씁니다.

---

## 7. 올리기 전에

```bash
python3 scripts/gen_index.py      # 루트 README 인덱스 갱신
python3 scripts/check_docs.py     # 링크·앵커·표·문체 검사
```

새로 쓰거나 크게 고친 문서는 경고까지 0건을 맞춥니다.

```bash
python3 scripts/check_docs.py --strict redis/
```

| 검사 | 결과 | 무엇을 잡나 |
|---|---|---|
| `gen_index.py` | README 갱신 | 카테고리 목록, 대표 문서, 실습 랩 표 |
| `gen_index.py --check` | 어긋나면 종료 코드 1 | CI에서 인덱스가 밀렸는지 |
| `check_docs.py` | 오류 시 종료 코드 1 | 깨진 링크, 없는 앵커, 중복 앵커, 표 열 수, H1 없음, 용어 |
| `check_docs.py --strict` | 경고도 실패 | 표 셀 종결어미, 문체 혼용 |

기존 문서 일부가 경고를 안고 있어 기본값은 경고입니다. CI는 **이번에 바뀐 문서만** 검사하므로,
과거 문서의 경고가 새 PR을 막지는 않습니다. 대신 손대는 문서는 그 자리에서 정리하고 갑니다.

---

## 참고

- [`scripts/gen_index.py`](scripts/gen_index.py) — 루트 README 인덱스 생성기. 대표 문서를 고르는 규칙이 상단 주석에 있습니다
- [`scripts/check_docs.py`](scripts/check_docs.py) — 문서 검사기. 각 규칙의 정규식이 그대로 들어 있습니다
- [`.github/workflows/docs.yml`](.github/workflows/docs.yml) — main 푸시 시 인덱스 자동 커밋, PR에서 변경 문서 검사
- [`memory/agent-memory/README.md`](memory/agent-memory/README.md) — 묶음 문서 허브의 기준 형태
- [`redis/azure-cache-to-managed-redis-migration.md`](redis/azure-cache-to-managed-redis-migration.md) — 메인 + 상세 분리와 실측 표기의 예
- [Microsoft Learn](https://learn.microsoft.com/ko-kr/azure/) — 제품 사양의 기준
