# CLAUDE.md

문서 작성 규칙은 **[AGENTS.md](AGENTS.md)** 한 곳에 있습니다. 이 저장소에서 문서를 만들거나 고치기 전에 먼저 읽으세요.

가장 자주 어기는 것만 다시 적으면 이렇습니다.

- 문서 하나에 문체 하나 — 절차·실습은 합니다체, 리서치·검증은 한다체
- 표 셀은 명사구로 끝냄 (`HA 복제본이 합산됨`, `HA 복제본이 합산됩니다` 아님)
- 루트 `README.md`의 문서 목록과 실습 랩 표는 **자동 생성**이므로 손으로 고치지 않음
- 절 번호는 문서마다 1부터, 다른 문서를 가리킬 때는 문서 이름을 함께
- 측정값에는 규모·리전·날짜를 붙이고, 고객 규모로 잼
- 이 저장소는 **공개**이므로 자격 증명·고객 식별 정보 금지

문서를 올리기 전에 두 가지를 돌립니다.

```bash
python3 scripts/gen_index.py                    # 루트 README 인덱스 갱신
python3 scripts/check_docs.py --strict <경로>   # 새로 쓴 문서는 경고까지 0건
```
