name: Documentation CI

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: |
            requirements-docs.txt
            samples/azure-monitor/source-material/sre-agent-event-lab/app/requirements-dev.txt

      - name: Install documentation and sample dependencies
        run: python -m pip install -r requirements-docs.txt -r samples/azure-monitor/source-material/sre-agent-event-lab/app/requirements-dev.txt

      - name: Run documentation and sample tests
        run: python -m pytest tests/docs samples/azure-monitor/source-material/sre-agent-event-lab -q

      - name: Validate public documentation
        run: |
          python scripts/docs/validate_metadata.py
          python scripts/docs/validate_sources.py
          python scripts/docs/validate_links.py
          python scripts/docs/validate_public_safety.py

      - name: Build site strictly
        run: mkdocs build --strict

      - name: Validate search index
        run: python scripts/docs/validate_search_index.py
